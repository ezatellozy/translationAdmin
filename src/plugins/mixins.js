// // import { getMainService } from "@/helpers";
// import Cookies from "js-cookie";
// import Vue from "vue";

// Vue.mixin({
// 	methods: {
// 		// getMainService,
// 		getTranslation(object, key) {
// 			const locale = Cookies.get("locale") || "en";
// 			return object[`${key}_${locale}`];
// 		}
// 	}
// });
